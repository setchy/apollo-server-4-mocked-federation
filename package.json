{
  "name": "apollo-server-4-mocked-federation",
  "version": "1.0.0",
  "description": "Apollo Server 4 mocked federation example",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --watch .env index.ts",
    "start": "npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Adam Setch <@setchy>",
  "license": "ISC",
  "dependencies": {
    "@apollo/gateway": "2.7.1",
    "@apollo/server": "4.10.0",
    "@faker-js/faker": "8.4.0",
    "@graphql-tools/mock": "9.0.1",
    "dotenv": "16.4.2"
  },
  "devDependencies": {
    "ts-node-dev": "2.0.0"
  }
}